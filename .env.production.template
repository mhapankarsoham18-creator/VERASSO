# VERASSO Production Environment Template

# 🔴 DANGER: NEVER COMMIT THE ACTUAL .env.production FILE TO GIT.
# This is a template only. Copy to .env.production and fill in the values.

# SUPABASE CONFIGURATION
# ----------------------
# Get these from your Supabase Dashboard -> Settings -> API
SUPABASE_URL=https://[YOUR-PROJECT-ID].supabase.co
SUPABASE_ANON_KEY=ey...

# DATABASE CONFIGURATION
# ----------------------
# Required if you choose to connect directly instead of using the API
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-ID].supabase.co:5432/postgres

# AI INTEGRATION
# --------------
# Required for CodeMaster and generic AI features
GEMINI_API_KEY=AI...

# PAYMENT GATEWAY (Phase 3)
# ------------------------
RAZORPAY_KEY_ID=rzp_live_...
RAZORPAY_KEY_SECRET=...

# PUSH NOTIFICATIONS (FCM)
# ------------------------
# These are usually baked into google-services.json / GoogleService-Info.plist,
# but if you need to access them programmatically:
# FCM_SERVER_KEY=AAA...

# SMTP CONFIGURATION (Configure in Supabase instead)
# ------------------------------------------------
# It's better to configure SendGrid/Resend directly in the Supabase Dashboard
# under Authentication -> Email templates -> Custom SMTP

# ENVIRONMENT CONFIG
# ------------------
FLAVOR=production
DEBUG_MODE=false
REQUIRE_MFA=true
