// ConversationListScreen tests require a live Supabase connection
// (Supabase.instance.client is called synchronously in initState).
// These tests are only runnable in integration test environment with a
// configured Supabase instance.
//
// Unit/Widget tests for this screen are intentionally skipped.
// See: test/features/messaging/crypto_service_test.dart
//      test/features/messaging/chat_screen_test.dart
// for the tests that cover E2E crypto and Chat UI logic.

void main() {
  // Intentionally empty — see comment above.
}
