import 'dart:ui';

/// A central repository of colors used throughout the CodeMaster Odyssey package.
class OdysseyColors {
  /// The main background color for screens and panels.
  static const Color background = Color(0xFF1A1A2E); // Deep Space/Night

  /// The primary brand color, used for buttons and key highlights.
  static const Color primary = Color(0xFFE94560); // Vibrant Red/Pink

  /// The secondary accent color for borders and backgrounds.
  static const Color accent = Color(0xFF0F3460); // Dark Blue

  /// Gold color used for rewards, badges, and progression paths.
  static const Color gold = Color(0xFFFFD700); // For rewards/paths

  /// The standard text color for high readability.
  static const Color text = Color(0xFFEDEDED);
}
