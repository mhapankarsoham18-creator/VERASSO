import 'package:flutter/material.dart';

/// Stubbed MeshRecsWidget (multiplayer removed).
class MeshRecsWidget extends StatelessWidget {
  const MeshRecsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // Hide recommendations in single player
  }
}
